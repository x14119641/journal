import pytest


"""
Need data to test this
@pytest.mar.asyncio
async def test_get_portfolio(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'
    
@pytest.mar.asyncio
async def test_get_summary_external(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'
    
@pytest.mar.asyncio
async def test_get_portfolio_ticker_aggregate(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'

@pytest.mar.asyncio
async def test_get_total_funds(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'

@pytest.mar.asyncio
async def test_get_allocation_funds(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'   

@pytest.mar.asyncio
async def test_get_portfolio_barchart_data(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !' 

@pytest.mar.asyncio
async def test_get_total_funds(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !' 

@pytest.mar.asyncio
async def test_add_funds(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !' 

@pytest.mar.asyncio
async def test_remove_funds(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !' 
"""