import pytest


"""
Need data to test this
@pytest.mar.asyncio
async def test_get_transactions(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'
    
@pytest.mar.asyncio
async def test_add_transaction(test_client):
    response = await test_client("")
    json_data = response.json()
    assert response.status_code == 200
    assert len(json_data) > 0, 'No tickers !'
"""